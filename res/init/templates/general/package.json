{
  "name": "{{name}}",
  "version": "0.1.0",
  "description": "{{description}}",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "{{author}}",
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "atscm": "^{{modulePackage.version}}"
  }
}
