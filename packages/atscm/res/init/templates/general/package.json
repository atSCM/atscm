{
  "name": "{{name}}",
  "version": "0.1.0",
  "private": true,
  "description": "{{description}}",
  "license": "UNLICENSED",
  "author": "{{{author}}}",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "atscm": "^{{modulePackage.version}}"
  }
}
